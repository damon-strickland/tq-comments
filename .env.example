# Copy this file to `.env` and fill in your service role key.
# Get the values from: Supabase dashboard → Project Settings → API

# Same Project URL you used in widget.js — the public one.
SUPABASE_URL=https://mvxdviqqtcvdtkenjvrc.supabase.co

# SECRET. Never commit this. The "service_role" key in the dashboard,
# not the anon key. It can read/write any bucket regardless of RLS.
SUPABASE_SERVICE_KEY=
